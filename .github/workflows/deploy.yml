name: Desplegar Web en GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Instalar dependencias
        run: flutter pub get

      # Comando crítico para generar sqflite_sw.js y sqlite3.wasm en la carpeta web/
      - name: Configurar binarios de SQLite Web
        run: dart run sqflite_common_ffi_web:setup --force

      - name: Compilar Flutter Web
        run: flutter build web --release --base-href "/rivera_grande/"

      - name: Desplegar a GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web