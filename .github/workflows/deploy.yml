name: Desplegar Web en GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Instalar dependencias
        run: flutter pub get

      - name: Configurar binarios de SQLite Web
        run: dart run sqflite_common_ffi_web:setup --force

      - name: Compilar Flutter Web
        run: flutter build web --release --base-href "/rivera_grande/"

      - name: Verificar y copiar binarios al build
        run: |
          cp -f web/sqflite_sw.js build/web/ || true
          cp -f web/sqlite3.wasm build/web/ || true

      - name: Desplegar a GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web